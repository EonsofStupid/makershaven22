
// This file is now deprecated - import from the new utils/settings/* files instead
import { 
  processDatabaseSettings,
  prepareDatabaseSettings,
  haveSettingsChanged 
} from './settings';

// Re-export for backward compatibility
export { 
  processDatabaseSettings,
  prepareDatabaseSettings,
  haveSettingsChanged
};
